# Dependencies / build output
node_modules
dist
.output
.vinxi
.wrangler
output

# Lockfiles — never reformatted
pnpm-lock.yaml
package-lock.json
bun.lock

# Generated by @tanstack/router-plugin on every dev/build; prettier must not
# fight the generator (see .gitattributes).
routeTree.gen.ts

# Locked golden/research data. These are byte-stable inputs the regression
# suite certifies against — formatting them would churn "locked" artifacts for
# zero benefit (README: regenerate the golden only on a deliberate change).
artifacts

# Lovable-managed agent instructions (<!-- LOVABLE:BEGIN --> … <!-- LOVABLE:END -->).
# Reformatted content can conflict with what the integration writes back.
AGENTS.md

# Compressed snapshot of the v1.3 delivery
System-v1.3.zip
